# steamer-plugin-ydj-add

YDJ基于steamerjs体系的命令插件

## USAGE

```shell

# 必须先安装steamerjs（如果没有），
$ npm i -g steamerjs

$ npm i -g steamer-plugin-ydj-add

# cd into your project baseDir
$ stream ydj-add
```